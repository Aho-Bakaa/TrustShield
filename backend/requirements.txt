# TrustShield backend dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
beautifulsoup4>=4.12
tldextract>=5.1
numpy>=1.26
soundfile>=0.12
litellm>=1.44
pymupdf>=1.24
pillow>=10.0

# Test suite
pytest>=8.0

# Optional — rendered-page evidence for phishing/social.
# The app degrades gracefully to an HTTP fetch if these are absent.
# After `pip install playwright`, run: python -m playwright install chromium
playwright>=1.45
