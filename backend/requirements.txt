# TrustShield backend dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
beautifulsoup4>=4.12
tldextract>=5.1
numpy>=1.26
soundfile>=0.12
litellm>=1.44
pymupdf>=1.24
pillow>=10.0

# Test suite
pytest>=8.0

# Optional — rendered-page evidence for phishing/social
playwright>=1.45
# Optional — domain intelligence (gracefully degrades if absent)
python-whois>=0.9
dnspython>=2.6

# Optional — Layer 1 voice spoof model (Wav2Vec2). Heavy; lazy-loaded.
# torch>=2.1
# transformers>=4.40
# Optional — Layer 3 voice ASR (faster-whisper preferred, openai-whisper fallback). Heavy; lazy-loaded.
# faster-whisper>=1.0
# openai-whisper>=20231117
