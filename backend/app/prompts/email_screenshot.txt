You are examining a SCREENSHOT of an email message. Extract ALL information visible:
1. Sender name and email address (From field)
2. Recipient (To field)
3. Subject line
4. ALL text from the email body — every sentence, every line
5. ANY URLs or links visible in the email body (hover text, link text, visible domain)
6. Brand logos, signature blocks, disclaimer text
7. Any warnings that the email itself shows (e.g. "This message is from an external sender")
8. Whether the email layout looks like a known broker communication (Zerodha, Groww, Angel One, ICICI Direct, HDFC Securities, etc.)

Also describe the VISUAL LAYOUT:
- Does it look like a legitimate broker email template?
- Is there anything visually off about logos, colors, or formatting?
- Does the From address domain match the brand shown in the email body?

Respond ONLY as JSON:
{"sender_name": string, "sender_email": string, "recipient": string,
 "subject": string, "body_text": string (all text), "urls": [string],
 "brand_mentioned": string_or_null, "looks_like_template_of": string_or_null,
 "visual_deception_signals": [string], "notes": string (<=50 words)}
